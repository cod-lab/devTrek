<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">

    <link rel="stylesheet" href="/stylesheets/index.css" type="text/css"/>
    <link rel="stylesheet" href="/stylesheets/footer.css" type="text/css"/>
    <link rel="stylesheet" href="/stylesheets/common.css" type="text/css"/>

    <title>Blogs</title>
</head>
<body onload="fromOtherPg('<%= activeLikeBtns %>','<%= srcPg %>')">
    <div class="container">
        <h1 class="mt-4 mb-4 text-center">devTrek<p class="d-inline blink">_</p> &#128187;</h1>
        <br>
        <div class="row">
            <div class="col" id="head"><h2>Blog Articles &#128218;</h2></div>
            <div class="col text-right" id="head"><a href="/articles/new" class="btn new" onclick="mouseClickNew('<%= activeLikeBtns %>')">New Article &#9997;</a></div>
        </div>

        <% articles.forEach(article => { %>
            <div class="card mt-4">
                <div class="card-body">
                    <h4 class="card-title"><%= article.title %></h4>

                    <div class="card-subtitle text-muted mb-2 createdAt">
                        <%= article.displayDate %>
                        <span>createdAt</span>
                    </div>

                    <div class="card-text mb-4"><%- article.modifiedDesc %></div>

                    <div class="row">
                        <div class="col">
                            <a href="/articles/<%= article._id %>" class="btn btn-primary" onclick="mouseClickShow('<%= activeLikeBtns %>')">Read More &#129488;</a>
                            <a href="/articles/edit/<%= article._id %>" class="btn btn-info" onclick="mouseClickEdit('<%= activeLikeBtns %>')">Edit &#128221;</a>
                            <form action="/articles/<%= article._id %>?_method=DELETE" method="POST" class="d-inline">
                                <button type="submit" class="btn btn-danger">Delete &#128465;</button>
                            </form>
                        </div>

                        <div class="col text-right mt-2">
                            <label id="<%= article._id %><%= article._id %>"><%= article.likes %></label>
                            <a href="#" class="likeBtn" id="<%= article._id %>"
                                onmouseover="mouseOverLike(this)"
                                onmouseout="mouseOutLike(this)"
                                onclick="mouseClickLike(this,'<%= activeLikeBtns %>')"
                            >
                                ♥
                                <span>like</span>
                            </a>
                            <label class="editor">By <%= article.username %></label>
                        </div>
                    </div>
                </div>
            </div>
        <% }); %>
    </div>
    <br>
    <%- include('_footer') %>       <!-- _footer common to all pages -->
    <script src="/scripts/index.js"></script>
</body>
</html>

